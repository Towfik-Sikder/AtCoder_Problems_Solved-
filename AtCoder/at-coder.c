#include <stdio.h>
int main(){
    int N;
    scanf("%d",&N);
    char a[20];
    for(char i='a'; i<='z'; i++){

    }

    if(N>=97 && N<=122){
            a[N];
        }


    return 0;
}

/*Problem Statement
The ASCII values of the lowercase English letters a, b, …, z are 97,98,…,122 in this order.

Given an integer N between 97 and 122, print the letter whose ASCII value is N.

Constraints
N is an integer between 97 and 122 (inclusive).

    int N;
    scanf("%d",&N);

     char myArray[200];

    for(char myArray='a';myArray<='z';myArray++){
        if(N>=97 && N<=122){
            char myArray[N];
            printf("%c\n",myArray[N]);
        }
    }


*/
